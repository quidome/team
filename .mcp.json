{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "nix",
      "args": [
        "develop",
        "path:.",
        "--command",
        "bash",
        "-c",
        "npx -y @playwright/mcp@latest --executable-path \"$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH\" --headless"
      ],
      "env": {}
    }
  }
}